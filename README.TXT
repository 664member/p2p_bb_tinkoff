📦 Установка

Перед запуском убедитесь, что у вас установлен Node.js
.

Установите зависимости через npm:

npm install puppeteer


Библиотеки fs, path и readline являются встроенными в Node.js, их устанавливать не нужно.

🚀 Запуск
node bybit.js


После запуска откроется браузер. Пройдите авторизацию на сайте.
Обязательно нажмите ENTER в консоли после авторизации, чтобы продолжить выполнение скрипта.

⚙️ Файлы проекта

bybit.js — основной скрипт

filters.json — хранит ваши фильтры и настройки

README.txt — текущий файл

.gitignore, package.json, package-lock.json — служебные файлы npm/git

🛠️ CLI-команды

Во время работы скрипта вы можете вводить команды прямо в консоль:

Команда	Действие
/setmin [число]	Устанавливает минимальную цену. Пример: /setmin 1000
/setamount [число]	Устанавливает сумму для фильтра. Пример: /setamount 200
/addkw [слово]	Добавляет ключевое слово. Пример: /addkw visa
/rmkw [слово]	Удаляет ключевое слово. Пример: /rmkw visa
/addban [слово]	Добавляет запретное слово (будет отфильтровано). Пример: /addban scam
/rmban [слово]	Удаляет запретное слово. Пример: /rmban scam
/addblack [ник]	Добавляет пользователя в чёрный список. Пример: /addblack user123
/rmblack [ник]	Удаляет пользователя из чёрного списка. Пример: /rmblack user123
/show	Показывает текущие фильтры в формате JSON
любая другая команда	Покажет сообщение: Неизвестная команда
📝 Примечания

Все изменения сохраняются в filters.json.

При каждом изменении фильтров данные автоматически сохраняются.

Скрипт использует Puppeteer для автоматизации браузера.



📬 Настройка уведомлений в Telegram

Для получения уведомлений в Telegram необходимо указать свои данные в файле bybit.js:

// ==== Telegram ====
const TELEGRAM_BOT_TOKEN = '';     // Ваш токен бота
const TELEGRAM_CHAT_ID = '';       // ID чата или @username канала

Где взять эти данные:

TELEGRAM_BOT_TOKEN — создайте бота через @BotFather
 и получите токен.

TELEGRAM_CHAT_ID:

Если это личный чат, можно узнать свой ID через бота @userinfobot
.

Если это групповой чат, добавьте туда своего бота и используйте специальный ID (обычно начинается с -100...).

Если это канал, добавьте бота как администратора и укажите @имя_канала.

⚠️ Без заполнения этих полей уведомления отправляться не будут.